<h1 class="text-center mb-4 text-4xl font-extrabold leading-none tracking-tight text-gray-900 md:text-5xl lg:text-6xl dark:text-white">Mach mit bei <%= @projects.size > 1 ? @projects.size : "diesem" %> <span class="text-transparent bg-clip-text bg-gradient-to-r to-emerald-600 from-sky-400"><%= @projects.size > 1 ? "Projekten" : "Projekt" %></span>!</h1>

<hr class="hidden sm:flex h-px my-8 bg-gray-200 border-0 dark:bg-gray-700">

<% @projects.each do |project| %>
  <%= render ProjectSectionComponent.new(project) %>
<% end %>
