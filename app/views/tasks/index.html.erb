<h1 class="text-center mb-4 text-4xl font-extrabold leading-none tracking-tight text-gray-900 md:text-5xl lg:text-6xl dark:text-white"><%= @tasks.size %> <span class="text-transparent bg-clip-text bg-gradient-to-r to-emerald-600 from-sky-400">Aufgaben</span> warten auf dich!</h1>


<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
  <% @tasks.each do |task| %>
    <%= render TaskCardComponent.new(task) %>
  <% end %>
</div>
